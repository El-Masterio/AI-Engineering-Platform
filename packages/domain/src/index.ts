/**
 * Pure business logic and domain entities. ZERO external dependencies by design (ADR-001, §19)
 *
 * See §19. Placeholder — M001 establishes the skeleton only.
 */
export const PACKAGE_NAME = "@atelier/domain" as const;
