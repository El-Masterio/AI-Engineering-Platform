/**
 * Token accounting, budget ceilings, and credit conversion (integer arithmetic only)
 *
 * See §7. Placeholder — M001 establishes the skeleton only.
 */
export const PACKAGE_NAME = "@atelier/cost" as const;
