{
  "extends": "@atelier/config/tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx"
  },
  "include": ["src/**/*"],
  "//": "Covers tests and stories so the editor and ESLint have type info. The BUILD uses tsconfig.build.json, which excludes them from emit."
}
