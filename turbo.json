{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "//": "^build is not optional. Workspace packages resolve through their exports map to dist/, which a clean clone does not have — without this, `pnpm dev` starts happily and serves 500 on every route with 'Cannot resolve @atelier/ui'. It only ever worked locally because a stale dist/ was lying around.",
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    }
  }
}
